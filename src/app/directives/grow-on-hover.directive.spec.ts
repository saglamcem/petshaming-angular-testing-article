import { GrowOnHoverDirective } from './grow-on-hover.directive';

describe('GrowOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new GrowOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
